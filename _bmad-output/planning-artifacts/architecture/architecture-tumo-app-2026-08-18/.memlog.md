---
scope: Feature: flujo QR + puntos por monto en módulo loyalty (scan empleado → sheet → tramos → confirmar; config de rangos; modelo puntos-nativo)
purpose: Build substrate para agentes de implementación: invariantes que eviten divergencia entre unidades construidas en paralelo
altitude: feature slice (brownfield: tumo-app Next.js 16)
updated: 2026-08-18T15:27
---

- (constraint) Brownfield tumo-app: Next 16.2.12 + React 19.2.4 + Tailwind 4 + bun test; shadcn/@base-ui/react; SQL crudo postgres.js; módulos con deps inyectadas {status,body}; sesión cookie session_token. No re-inventar stack.
- (constraint) Intent binding: 8 decisiones de brainstorm-loyalty-qr-scan-flow-2026-08-18 (confirmación 1-tap, rangos config, puntos-nativo pizarra limpia, canje+sumar en sheet, acciones employee-only, QR=URL pública, debajo-mínimo no registra, código 4d plan B).
- (constraint) TDD obligatorio del repo; tests existentes en tests/loyalty-*.test.ts y tests/ui/loyalty-*.test.tsx a extender/adaptar.
- (decision) AD-1 Paradigma: modular monolith por dominio. Loyalty core en modules/loyalty (api+lib+dashboard+public); shell/db + app/api routes thin; UI shell en components/ui. Prevents: lógica de negocio en route handlers o componentes.
- (decision) AD-2 Modelo puntos-nativo pizarra limpia: customers.purchases→points, total_purchases→total_points; businesses.purchases_needed→points_needed. Tabla purchases→point_movements (id, customer_id, employee_id, business_id, points INT, amount_cents INT NULL, range_label TEXT NULL, kind earn|redeem, created_at). Migración 003; sin dual-write ni compat layer.
- (decision) AD-3 Rangos en JSONB businesses.point_ranges: array ordenado de {min_cents, max_cents|null, points}. Contiguos, último abierto. Validación server-side en PATCH program.
- (decision) AD-4 QR payload = URL canónica /{slug}/loyalty/c/{code}. Generación con qrcode@1.5.4 ya instalada.
- (decision) AD-5 Gate de acciones: sumar/canjear SOLO con sesión employee/owner validada server-side. Página pública del QR sin sesión = tarjeta read-only; con sesión employee del mismo business = redirect a dashboard scan sheet.
- (decision) AD-6 Vista principal employee loyalty = scanner in-app en /{slug}/dashboard/loyalty. Lista/código 4d vía link secundario ¿No funciona el QR?.
- (decision) AD-7 Sheet de acción = components/ui/sheet.tsx side=bottom. Un solo CustomerActionSheet. No instalar vaul/drawer.
- (decision) AD-8 Scanner lib = @yudiel/react-qr-scanner@2.6.0. Parse URL canónica; rechazo amable de QR ajenos; distingue cliente vs registro.
- (decision) AD-9 Anti-duplicado: client debounce 2s post-confirm + server si último earn mismo customer+business <60s → 409 DUPLICATE_RECENT y UI confirmación extra.
- (decision) AD-10 API: POST /api/loyalty/points reemplaza purchases; redemptions se mantiene (reset points→0 + movement redeem); PATCH program con point_ranges+points_needed+reward_name; GET customers fields points.
- (decision) AD-11 Deps: app/api→modules/loyalty/api+default-deps; dashboard|public→lib+components/ui; api NO importa React; shell/db solo desde default-deps y migrations.
- (decision) AD-12 Montos en amount_cents INT (centavos). UI formatea a miles. Sin float money.
- (version) next@16.2.12 react@19.2.4 @base-ui/react@^1.7.0 qrcode@1.5.4 postgres@3.4.9 @yudiel/react-qr-scanner@2.6.0 typescript@5 bun test
- (assumption) [ASSUMPTION] Rangos JSONB en businesses (no tabla loyalty_ranges).
- (assumption) [ASSUMPTION] Deep-link: /{slug}/loyalty/c/{code} con sesión employee mismo business → redirect /{slug}/dashboard/loyalty?c={code} abre sheet.
- (assumption) [ASSUMPTION] Ventana anti-dupe=60s; umbral premio=points_needed.
- (assumption) [ASSUMPTION] No drag-to-dismiss sheet MVP.
- (assumption) [ASSUMPTION] Lista clientes se conserva vía plan B, no se borra.
- (assumption) [ASSUMPTION] amount_cents en earn = min_cents del tramo elegido (representativo) + range_label; no se pide monto exacto al empleado.
- (event) Fast path: decisions drafted from intent+code review; ready to distill spine.
- (decision) AD-3a Earn body only {customerId, rangeIndex, force?, expectedPoints?}; server resolves band; 409 RANGE_CHANGED if expectedPoints mismatch.
- (decision) AD-10a Single-tx FOR UPDATE: earn +=points+total_points; redeem require threshold then points=0 + movement kind=redeem; drop redemptions table.
- (decision) AD-3 tightened: contiguous validation rules enumerated; leading 0-pt band allowed once.
- (event) Reviewer gate: lint low-only false positives; rubric PASS_WITH_FIXES applied; tech PASS; adversarial FAIL holes closed (dual body, dual ledger, tx algebra, range validation).
- (event) spine finalized
